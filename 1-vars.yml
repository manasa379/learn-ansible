- name: Demo on printing
  hosts: localhost
  tasks:
    - name: Print Hello world
      ansible.builtin.debug:
        msg: Hello world

- name: Demo on vars
  hosts: localhost
  Vars:
    URL: numbers.com
  tasks:
    - name: print a variable
      ansible.builtin.debug:
        msg: "{{7}}"